{
  "name": "freechess-e2e",
  "private": true,
  "version": "0.1.0",
  "description": "Two-node browser tests for FreeChess",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
